# Endfield Calculator

一个用于规划行列目标的轻量级桌面求解器，支持 3×3 到 6×6 棋盘，以及游戏中已经存在、无法部署的初始障碍格。

## 使用方法

1. 运行 `python main.py`。
2. 点击棋盘上方和左侧的数字设置每列、每行的目标数；右键可以反向调整。
3. 点击棋盘格添加或移除初始障碍。
4. 点击“开始求解”，再用“上一个 / 下一个”浏览全部方案。

障碍格显示为 `×`，求解后的部署位置显示为绿色 `●`。

## 测试

```bash
python -m unittest -v
```
